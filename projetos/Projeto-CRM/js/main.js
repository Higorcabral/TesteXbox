/* =========================================================
   Rubra CRM, Bootstrap
   Ponto de entrada. Aguarda o DOM e chama o controller.
   ========================================================= */

document.addEventListener('DOMContentLoaded', function () {
  window.CRM.AppController.init();
});
